name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    name: Lint, typecheck, test, build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      fail-fast: false
      matrix:
        node-version: [20.x, 22.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci --no-audit --no-fund
      - run: npm run typecheck
      - run: npm run test:unit -- --coverage
      - run: npm run build
      - run: npx publint
      - run: npx attw --pack .

  spec-snapshot:
    name: Verify Next.js 16 spec snapshot
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      # Manual review checkpoint: when this hash diverges, re-read
      # docs/next16-spec.md against the upstream docs and update.
      - name: Print snapshot fingerprint
        run: |
          sha256sum docs/next16-spec.md
